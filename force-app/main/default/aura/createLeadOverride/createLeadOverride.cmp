<aura:component implements="lightning:actionOverride" description="Overrides the Standard New button on Lead to show Create Lead modal (LWC)">
    <lightning:navigation aura:id="navService"/>
    <c:createLeadModal
        onsuccess="{!c.handleSuccess}"
        oncancel="{!c.handleCancel}" />
</aura:component>